プライベートリポジトリはそのままでは `vercel link` できない。

VercelのGitHub Appをインストールする必要がある。
https://github.com/apps/vercel

GitHub Appの一覧から `Configure` ボタンで設定を開ける。
https://github.com/settings/installations
Repository accesssはOnly select repositoriesで都度設定した方がいい。

あとはいつも通り。
