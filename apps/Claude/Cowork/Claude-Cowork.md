## Codeとの違い

CLIではなく、デスクトップアプリで使う。より一般向け。
システム上で直接実行するのではなく、仮想環境で実行するので、比較的安全。

## その他

アプリやファイルを操作して、なんらかのまとまった処理を実行するのに適している。

例えば、

1. ダウンロードフォルダにあるPDFファイルを領収書フォルダに移動して、
2. すべての領収書から日付、金額、税、支払い先を抽出して、
3. 領収書一覧.xlsxを作って、
4. 処理した領収書のファイル名の末尾に「処理済」を追加する

このような処理を自然言語で書ける。
