プロジェクトを削除しても生成された画像は残る。

画像ファイルの名前に入るプロンプトは省略されている。
完全なスクリプトを残したいならプロジェクトをエクスポートするか、テキストファイルなどに残す。

「バッチサイズ」で複数回実行する場合は並行処理、「生成」の横の回数設定で複数回実行する場合は直列処理になる。
回数設定の場合はスキップもできるし、30回以上連続でも動かせる。特に理由がなければ直列実行でいい。
どちらの場合でもシードは自動的にランダム生成になる。
