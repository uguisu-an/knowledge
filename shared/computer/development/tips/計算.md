計算周りは表示と内部処理で同じパッケージを使えるように、クライアントとサーバーが同じ言語になっていると扱いやすい。今なら WebAssembly もあるか。

お金や科学計算のように厳密な結果が必要なら Decimal を使うように。

表示用のサマリの計算はクライアントで実施して、サーバーでは元になる値だけ扱う。
ただ、計算量が多すぎるとか、集計したいとか、特定の目的でキャッシュを保存することはある。その場合、クライアント側の送信内容は信用せずにサーバー側であらためて集計する。
