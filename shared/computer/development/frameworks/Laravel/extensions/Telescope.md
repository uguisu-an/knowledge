# Laravel Telescope

ロギングとそれを閲覧するビューがセットになった拡張機能。
導入するだけでアクセスログやクエリログ、その他のテキストログなど様々なログを記録できる。

デフォルトでは production 環境でほとんどのログは取らないように設定される。基本的に staging 環境でのデバッグ用。

検索が弱い。
`TelescopeProvider`を設定してタグをつけないと、タグの完全一致以外で検索できない。
