## POST

コレクションにオブジェクトを追加する。

RESTでは、idを指定せずに「コレクションにオブジェクトを追加する」のがPOSTの役割となっている。
対して、PUTやPATCHはidを指定する。ただ、実装を分けるのが面倒ならPATCHをPUTとして実装してしまうこともある。

## PUT

単一のリソースを指定して、オブジェクトを置き換える。

## PATCH

単一のリソースを指定して、オブジェクトの一部を変更する。
