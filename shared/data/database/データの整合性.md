整合性を取るべきでないデータもある。
「過度な正規化」は、値がずれることもあるのに、共通化してしまったパターン。
ただ、原則としては、データはできるだけ正規化できるように仕様を考えたほうがいい。その上で例外的に正規化しない場合がある。

あえてデータベース側で整合性を取らずにアプリケーション側で整合性を取る方法もある。特にテーブル間の状態にズレを許容する場合。例えばタグの候補は出すけど、候補から消えてもタグは消さないとか。まぁ、その場合はそもそもタグとタグの候補のテーブルは分けた方がいい。
