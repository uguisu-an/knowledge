一つの画面に機能を詰め込む場合、ある程度区画を区切った上で個別に読み込んで表示した方がユーザー視点では軽い。
システム視点ではそもそもたくさん読み込まれて重いので、できればページごと分けた方がいい。キャッシュできるような機能なら影響は少ないけど、あまりない。
あとはSSGで一度生成したらある程度キャッシュできるなら大した負担ではないかも。

画面の移動は少ない方が楽な場合もある。

画面を分けることで文脈を分けられる。
必要以上の情報を出さないのもユーザーの負荷を下げる。
