# テストの限界

テストは品質を確かめるだけ。見えるようにするだけ。

テストしないと品質はわからない。

製品の品質を高めるのはテストではない。
何度もリリースとレビューを繰り返して、仕様を更新すること。

品質を確かめるのと速度を両立して品質を高めるためには、テストを機械化するところまでが必要。

テストは品質を確かめる。明らかにする。
テストしないと品質がわからないので、品質を高められているかわからない。
