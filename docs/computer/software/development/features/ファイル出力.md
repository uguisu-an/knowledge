日次出力するファイル名に日付をつける場合、ミリ秒までつけたほうが1日の出力回数が変わった場合にも対応しやすい。
現行の仕様では日次でも、10年後までそうとは限らない。
年次、月次、なんかも同じ。

ファイルに出力する内容はデータベースにも出力した方がいい。
ユーザーがダウンロードする場合でもあとから確認できるし、再出力する場合の計算が軽くなる。
手順としては、idで対応するデータを探せるようにして、データがなかったら計算してデータベースに入れつつ出力、データがあったらデータベースから取り出して出力。
