# モデリングと設計との違い

モデリングもモデルとモデルの間に境界を引く意味では design に近い。

ただ、モデルは interface とは限らない。単に border というべきか。
境界と言っても、接点があるとは限らない。境界よりも領域が重要。

interface には装飾も含まれるので、modeling と design のどちらかがもう一方を内包する関係ではない。

また、モデルというと interface も含めた一個を考える。「個」の概念が modeling の特徴かも。何らかの「差」もある。
